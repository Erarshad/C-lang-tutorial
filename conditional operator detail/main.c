#include<stdio.h>
#include<Conio.h>
int main()
{

    int a[],n,m.,b[];
    printf("enter the size");
    scanf("%d",&n);
    printf("enter the element");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    printf("enter the length of second element ");
    scanf("%d",&m);
    printf("enter the element:");
    for(i=0;i<m;i++)
    {
        scanf("%d",&b[i])
    }
      printf("stored elments in set a");
      for (i=0;i<n;i++)
      {
          printf("%d",a[i]);
      }
     printf("stored elements in set b");
     for (i=0;i<m;i++)
     {
         printf("%d",
     }
}
